frame = "none"         # Полностью убирает рамку и заголовок
# ИЛИ
# frame = "buttonless"  # Оставляет рамку, но убирает кнопки закрытия/сворачивания
# frame = "transparent" # Делает панель прозрачной (macOS-only)

title-hidden = true     # Скрывает текст заголовка (доступно с версии 0.12.2)

