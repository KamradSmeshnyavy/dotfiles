# # Source the theme.conf which contains color definitions
# source-file ~/.config/tmux/theme.conf
# Загружаем переменные темы Omarchy
if-shell 'test -f ~/.config/tmux/om-theme.conf' {
  source-file ~/.config/tmux/om-theme.conf
}
# Load keybindings
source-file ~/.config/tmux/keybind.conf

# Включает поддержку расширенных клавиш (Kitty Keyboard Protocol)
set -as terminal-features ',xterm-kitty:extkeys'
set -as terminal-features ',kitty:extkeys'
set -g default-terminal "xterm-kitty"

set -g update-environment "DISPLAY SSH_ASKPASS SSH_AUTH_SOCK SSH_AGENT_PID SSH_CONNECTION WINDOWID XAUTHORITY TERM_PROGRAM"

set-environment -g TERM_PROGRAM "kitty"
# Передавать escape-последовательности напрямую
set -s extended-keys on

set -g mouse on
set -g base-index 1
set -g pane-base-index 1
set -s escape-time 0
set -g history-limit 30000
set -g display-time 2000
set -g focus-events on
setw -g aggressive-resize on
set -g set-clipboard external
set-window-option -g pane-base-index 1
set-option -g renumber-windows on
set -g detach-on-destroy off
# set -g default-shell /sbin/nu
# set -g default-command "/sbin/nu -l"
# set -as terminal-features 'xterm*:extkeys'

# ╭───────────────────────────────────────────────────────╮
# │                      plugins                          │
# ╰───────────────────────────────────────────────────────╯
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-yank'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @plugin 'sainnhe/tmux-fzf'#TODO:check
set -g @plugin 'wfxr/tmux-fzf-url'#TODO:check
set -g @plugin 'omerxx/tmux-sessionx'
set -g @plugin 'omerxx/tmux-floax'
set -g @plugin 'christoomey/vim-tmux-navigator'
# floax
set -g @plugin 'omerxx/tmux-floax'
set -g @floax-width '80%'
set -g @floax-height '80%'
set -g @floax-border-color 'magenta'
set -g @floax-text-color 'blue'
set -g @floax-bind '-n M-f'
# set -g @floax-bind '-n M-p'
set -g @floax-change-path 'false'
# flash-copy
set -g @plugin 'kristijan/flash-copy.tmux'

#set -g @plugin 'jeffnguyen695/tmux-zoxide-session'#TODO:check

set -g @sessionx-bind-zo-new-window 'ctrl-y'
set -g @sessionx-auto-accept 'off'
set -g @sessionx-custom-paths '/Users/kamradsmeshnyavy/dotfiles'
set -g @sessionx-bind '-T SESSION_MODE y'
set -g @sessionx-x-path '~/dotfiles'
set -g @sessionx-window-height '85%'
set -g @sessionx-window-width '75%'
set -g @sessionx-zoxide-mode 'on'
set -g @sessionx-custom-paths-subdirectories 'false'
set -g @sessionx-filter-current 'false'

set -g @continuum-restore 'on'
set -g @resurrect-strategy-nvim 'session'

# set -g @plugin 'Morantron/tmux-fingers'
# run-shell ~/dotfiles/tmux/plugins/tmux-fingers/tmux-fingers.tmux
# set -g @fingers-key '-T PANE_MODE f'

set -g @plugin 'schasse/tmux-jump'
set -g @jump-key 's'
set -g @jump-bg-color '\e[0m\e[90m'
set -g @jump-fg-color '\e[1m\e[31m'
set -g @jump-keys-position 'left'


# ╭───────────────────────────────────────────────────────╮
# │                   Appearance                          │
# ╰───────────────────────────────────────────────────────╯

# set -g pane-border-status top
# set -g pane-border-format "#P"
# set -g pane-border-lines simple
# set -g pane-border-style "bg=default,fg=#{@thm_overlay_0}"
# set -g pane-active-border-style "bg=default,fg=#{@thm_blue}"
# # status bar
# set -g status-position top
# set -g status-left-length 100
# set -g status-style "bg=default"
# set -g status-justify "absolute-centre"
#
# # Status Bar - Left - Session Name, Current Path
# set -g status-left ""
# set -ga status-left "#{?client_prefix,#{#[fg=#{@thm_red},bold]  #S },#{#[fg=#{@thm_green}]  #S }}"
# set -ga status-left "#[fg=#{@thm_overlay_0},none]│"
# set -ga status-left "#[fg=#{@thm_blue}]  #{=/-32/...:#{s|$USER|~|:#{b:pane_current_path}}} "
#
# # Status Bar - Center - Windows
# set -wg automatic-rename on
# set -g automatic-rename-format "#{pane_current_command}"
# set -g window-status-format " #W "
# set -g window-status-style "fg=#{@thm_text}"
# set -g window-status-current-format " #W "
# set -g window-status-current-style "fg=#{@thm_mauve},bold"
# set -gF window-status-separator "#[fg=#{@thm_overlay_0}]│"
#
# # Status Bar - Right - Current Command
# set -g status-right ""
# set -ga status-right "#[fg=#{@thm_maroon}]  #{pane_current_command} "
#


set -g pane-border-status top
set -g pane-border-format "#P"
set -g pane-border-lines simple
set -g pane-border-style "[bg=#{@om_red},fg=#{@om_bg},bold]"
set -g pane-active-border-style "[bg=#{@om_red},fg=#{@om_bg},bold]"
# status bar
set -g status-position top
set -g @catppuccin_window_status_style "rounded"
set -g @catppuccin_window_number_position "right"

set -g @catppuccin_window_default_fill "number"
set -g @catppuccin_window_default_text "#W"

set -g @catppuccin_window_current_fill "number"
set -g @catppuccin_window_current_text "#W"

set -g @catppuccin_status_fill "icon"
set -g @catppuccin_status_connect_separator "no"

set -g @catppuccin_directory_text "#{pane_current_path}"

# set -g @catppuccin_flavor "mocha"
set -g @catppuccin_status_background "none"
set -g @catppuccin_window_status_style "none"
set -g @catppuccin_pane_status_enabled "off"
set -g @catppuccin_pane_border_status "off"

set -g status-left-length 100
set -g status-left ""
set -ga status-left "#{?client_prefix,#{#[bg=#{@om_red},fg=#{@om_bg},bold]  #S },#{#[bg=#{@om_bg},fg=#{@om_lavender}]  #S }}"
set -ga status-left "#[bg=#{@om_bg},fg=#{@om_overlay_0},none]│"
set -ga status-left "#[bg=#{@om_bg},fg=#{@om_blue}]  #{=/-32/...:#{s|$USER|~|:#{b:pane_current_path}}} "
set -ga status-left "#[bg=#{@om_bg},fg=#{@om_overlay_0},none]#{?window_zoomed_flag,│,}"
set -ga status-left "#[bg=#{@om_bg},fg=#{@om_yellow}]#{?window_zoomed_flag,  zoom ,}"

set -g status-right ""
set -ga status-right "#[bg=#{@om_bg},fg=#{@om_maroon}]  #{pane_current_command} #[bg=#{@om_bg},fg=#{@om_maroon}]#{?#{==:#{client_key_table},root},NORMAL,#{client_key_table}} #[bg=#1e2030,fg=#82aaff,nobold,nounderscore,noitalics] "

set -g status-style "bg=#{@om_bg}"
set -g status-justify "absolute-centre"

set -wg automatic-rename on
set -g automatic-rename-format "#{pane_current_command}"

set -g window-status-format " #I#{?#{!=:#{window_name},Window},: #W,} "
set -g window-status-style "bg=#{@om_bg},fg=#{@om_rosewater}"
set -g window-status-last-style "bg=#{@om_bg},fg=#{@om_rosewater}"
set -g window-status-activity-style "bg=#{@om_red},fg=#{@om_bg}"
set -g window-status-bell-style "bg=#{@om_red},fg=#{@om_bg},bold"
set -gF window-status-separator "#[bg=#{@om_bg},fg=#{@om_overlay_0}]│"

set -g window-status-current-format " #I#{?#{!=:#{window_name},Window},: #W,} "
set -g window-status-current-style "bg=#{},fg=#{},bold"

# Initialize TMUX plugin manager;
run '~/.config/tmux/plugins/tpm/tpm'

